# ATLAS Environment Variables

# Database (shared by both application and Temporal)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=atlas
DB_USER=postgres
DB_PASSWORD=postgres

# Temporal
TEMPORAL_HOST=localhost
TEMPORAL_PORT=7233
TEMPORAL_NAMESPACE=default
# Temporal uses the postgres user but with a separate database
TEMPORAL_DB_NAME=temporal

# API
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=true
